{
  "name": "opdracht_api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "vite --host --open",
    "prepreview": "npm run build",
    "preview": "vite preview --host --open",
    "prebuild": "rm -rf ./dist",
    "build": "vite build --base ./",
    "deploy": "surge ./dist fe-cc-api.surge.sh",
    "clean": "rm -rf ./dist ./node_modules"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.3.2"
  }
}
